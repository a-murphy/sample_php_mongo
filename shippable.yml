language: python

build:
  pre_ci:
    - docker ps
  pre_ci_boot:
    image_name: drydock/u14pyt
    image_tag: prod
    pull: true
    options: "--net=host"
  ci:
    - docker ps
    - mkdir -p shippable/testresults
    - mkdir -p shippable/codecoverage
